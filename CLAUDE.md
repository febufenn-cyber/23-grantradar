# CLAUDE.md

This repo is product #23 of Febin's 50-SaaS challenge. Nothing is built yet — `docs/` is the source of truth. Read `docs/LLD.md` then `docs/PLAN.md` and execute tasks in order with TDD.

Stack conventions + the shipped reference implementation live in the private repo `febufenn-cyber/50-saas` (`contract-reviewer/`) — same Worker+Supabase patterns, adapted here to a cron-indexer + monthly-quota subscription instead of per-operation credits.

Model strings, the pre-index-never-live-proxy decision, and the license/data constraints in the LLD (NIH PI emails, CORDIS key gate) are verified decisions — do not re-litigate without evidence.
